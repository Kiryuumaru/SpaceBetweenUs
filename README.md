# SpaceBetweenUs
Project description

## Getting Started
1) Create a virtual python environment `v3.7 x64` named `env` for `SpaceBetweenUs` project
2) Install all requirements in `requirements.txt` file into virtual python environment
3) Run `Additionals.bat` to download yolo additional files and sample videos

<UserControl x:Class="SpaceBetweenUs.Views.Pages.Logs"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             mc:Ignorable="d" >
    <Grid Style="{StaticResource ContentRoot}">
        <ScrollViewer>
            <DataGrid ItemsSource="{Binding ViolationLogs}" AutoGenerateColumns="False" PreviewKeyDown="DataGrid_PreviewKeyDown">
                <DataGrid.Columns>
                    <mui:DataGridTextColumn Header="Date" Binding="{Binding DateString, Mode=OneWay}" />
                    <mui:DataGridTextColumn Header="Time" Binding="{Binding TimeString, Mode=OneWay}" />
                    <mui:DataGridTextColumn Header="Violations" Binding="{Binding ViolationsCount, Mode=OneWay}" />
                    <mui:DataGridTextColumn Header="Violators" Binding="{Binding ViolatorsCount, Mode=OneWay}" />
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
    </Grid>
</UserControl>
